<%- include('../partials/header.ejs')%>
    <div class = "content">
        <h4><%= story.title%></h4>
        <small>posted <%=story.createdAt%> by <%=story.author%></small>
        <div id ="story">
            <%=story.content%>
        </div>
        <form>
            <button type = "submit" formmethod="GET" formaction="/stories/<%= story.id%>/edit" >Edit Story</button>
            <button type = "submit" formmethod="POST" formaction="/stories/<%= story.id%>?_method=DELETE" >Delete Story</button>

        </form>
    </div>

    <%- include('../partials/footer.ejs')%>
